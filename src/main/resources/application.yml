#SERVER Port
server:
  port: '9000'

spring:
  datasource:
    url: ${DATABASE.URL}
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: 'true'
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update